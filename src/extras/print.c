// This file will be removed when we link the standard library

#include <stdio.h>

void printint(int x) {
    printf("%d\n", x);
}

void printchar(char c) {
    printf("%c\n", c);
}

void printstr(char *s) {
    printf("%s\n", s);
}